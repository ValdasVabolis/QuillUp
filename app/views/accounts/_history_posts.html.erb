<h2>Post history</h2>
<% current_user.posts.each do |post| %>
  <div class="well">
    <%= render partial: "shared/post_actions", locals: { post: post} %>
    <h4><%= link_to("#{post.title}", users_post_path(post)) %></h4>
    <p><%= post.body %></p>
  </div>
<% end %>
