<% if user_signed_in? %>
    <div class="container-fluid">
        <div class="row-fluid">
            <div class="col-md-4 col-md-offset-4 text-center">
                <h1>You're logged in!</h1>
                <%= link_to "Create a new post", "#", class: "btn btn-default" %>
            </div>
        </div>
    </div>
<% else %>
    <h1 class="text-center">Welcome to Quill!</h1>
<% end %>