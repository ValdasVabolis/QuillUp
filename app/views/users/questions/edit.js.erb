var text = $('#body').hide().text();
var question = $('.question-large[data-id="<%= @question.id %>"]')

if(question.hasClass('editing')) {
	question.removeClass('editing');
	$('#body').show();
} else {
	question.addClass('editing');
	$('#area').show();
	$('#area').val(text).focus();
}


autosize($('#area'));