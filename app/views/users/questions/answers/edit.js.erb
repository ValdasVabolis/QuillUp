var answer = $('.question-answer[data-id="<%= @answer.id %>"]');
var form = answer.find('.form_answer');
var body = answer.find('.answer-group');
var area = form.find('#area-answer-edit');
var text = body.text();

if(answer.hasClass('editing')) {
	answer.removeClass('editing');
} else {
	answer.addClass('editing');
	//area.val(text).focus();
  Text.limit({
    text_selector: area,
    count_selector: '#area-answer-counter-edit',
    limit: 1000
  });
}
autosize(area);

