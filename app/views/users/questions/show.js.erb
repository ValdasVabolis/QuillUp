Popover.show("<%= j render(file: 'users/questions/show.html.erb', locals: { question: @question }) %>");
$('#popover-view-contents').on('click', function(e) {
  e.stopPropagation();
});
$(document).on('click', Popover.exit);