var form = '<%= j render(template: "/users/posts/comments/new", layout: false, locals: { post: @comment.post }) %>]';
var comment = '.post-comment[data-id=<%= @comment.id %>]';
if($(comment).next().attr('id') != 'comment-form') {
  $(form).insertAfter($(comment));
}
